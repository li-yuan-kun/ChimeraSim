"""Tiling interface."""

from chiplet_hetero_sim.workload.graph import WorkloadGraph


def tile_partition(graph: WorkloadGraph) -> WorkloadGraph:
    """Convert partition into tile-level workload (stub)."""
    return graph
